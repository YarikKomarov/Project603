spring:
  application:
    name: order-service

server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/orderdb
    username: user
    password: password

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/